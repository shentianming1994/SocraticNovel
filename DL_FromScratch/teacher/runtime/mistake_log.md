# 错题本

> 延迟加载（查错题记录时读取）。记录学习者答错或卡住的题目/概念，用于针对性复习。

---

## 记录格式

| 日期 | 章节 | 知识点 | 错误类型 | 错的内容 | 正确理解 | 复习状态 |
|------|------|--------|---------|---------|---------|---------|

错误类型分类：
- **概念误解**：对概念本身的理解错误（如「梯度=误差」）
- **代码实现**：概念懂但代码写错（如维度/转置错误）
- **计算失误**：推导或计算中的具体错误
- **陷阱踩中**：踩了预设的教学陷阱（同时记入 session_log 的陷阱结果）

---

## 错题记录

| 日期 | 章节 | 知识点 | 错误类型 | 错的内容 | 正确理解 | 复习状态 |
|------|------|--------|---------|---------|---------|---------|
| 2026-06-23 | Ch.1 | shape 含义 | 陷阱踩中 | 把 `img.shape` 当成像素总数（3×3=9） | shape 给的是 (排数, 列数) 两个数，描述方块的两条边；总数才是两边相乘 | 已课中自行修正，进 review_queue 巩固（到期 06-25） |
| 2026-06-23 | Ch.1 | 多位数 vs 多个数 | 概念误解 | `np.array(123,456,789)` 以为 `123` 是三个数 | `123` 是一个整数（一百二十三）；要九个格子须写九个独立的数 | 课中经"数几个数"追问自行修正 |
| 2026-06-23 | Ch.1 | 嵌套括号/分隔 | 代码实现 | 漏外层 `[]`（三排当三个参数→TypeError）；误用 `{}`（SyntaxError 括号不配）；排间漏逗号 | 整个网格是"一个"东西，须最外层再套一对 `[]`；Python 用 `[]` 不用 `{}`；排与排之间用逗号分隔 | 课中借报错信息逐条自行读懂修正 |
| 2026-06-23 | Ch.1 | 取元素语法 | 代码实现 | `img.(2,2)` 用点+圆括号取位置 | 取位置用方括号 `img[2,2]`；点 `.` 用来取有名字的属性（如 `.shape`） | 课中修正 |
| 2026-06-23 | Ch.1 | 矩阵乘构成 | 计算失误 | 凑 19 时写 `3*2*3+1`（三数相乘、来源混乱） | 矩阵乘是 A 一排 × B 一列对应相乘再求和：`1×5+2×7=19` | 课中经"只用这两组数"收束后理解 |
| 2026-06-23 | Ch.1 | 广播条件 | 概念误解 | 以为广播条件是"大的能被小的整除"（(6,)+(3,)） | 条件是对应维度相等、或其中一边为 1；6 与 3 不满足，故 ValueError | 课中经反例 ValueError 自行修正为正确条件 |
| 2026-06-23 | Ch.1 | `np.arange` 区间 | 概念误解 | 把 `arange(0,6,0.1)` 读成"到 6"（含端点） | 右开区间：到 end 之前停，最后一个是 5.9 不是 6；end 是"上限/不能达到" | 课中经指尖回看末位数自行修正，进 review_queue（到期 06-25） |
| 2026-06-23 | Ch.1 | 变量复用/计数 | 计算失误 | 数 y 的个数时忘了 x 已定义、临时假设"x 只能是整数" | x 即先前 `arange` 那 60 个 0.1 步长数；变量定义后持续存在，y=sin(x) 个数 = x 个数 = 60 | 课中经"往上看 0.1 是整数吗"提示瞬间自纠 |
| 2026-06-23 | Ch.1 | 环境/包管理 | 代码实现 | `pip install` 在 Python 解释器内跑（SyntaxError）；launcher 路径报错 | pip 在终端（解释器外）跑，先 `exit()`；路径异常时用 `python -m pip install` 绕过 launcher | 课中经两步提示修复，matplotlib 装成 |

---

## 高频错误追踪

> 同一类错误出现 ≥ 3 次时，在此标注，提醒当课老师重点关注。

（暂无）
