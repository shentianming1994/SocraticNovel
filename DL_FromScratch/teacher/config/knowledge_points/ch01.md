# Ch.1 — Python 入门（知识点）

> **教材路径**：`materials/textbook/dl_from_scratch.md` 第1章  
> **老师**：更科 瑠夏 ｜ **情感阶段**：初期·距离  
> 标记：`[ ]` 未覆盖 ｜ `[~]` 部分/待巩固 ｜ `[x]` 已掌握（概念+代码双轨）

---

## 学习目标（LO）

- [~] **LO1.1 Python/NumPy 环境与基本语法**
  - 概念：[~] ｜ 代码：[~]
  - 要点：变量、列表、字典、import；能跑起来一段最简代码
  - 备注：已亲手覆盖 `import ... as`、变量赋值（`img=`/`a=`）、列表/嵌套列表 `[[...],[...]]`、读懂报错（TypeError/SyntaxError/ValueError/ModuleNotFoundError）。06-23 晚补：`np.arange` 步长/区间、变量复用（x 在多行间持续存在）、包管理 `python -m pip install`（终端 vs 解释器内的区别、ModuleNotFoundError 实战）。字典、更广的语法仍未碰。
- [x] **LO1.2 NumPy 数组（ndarray）**
  - 概念：[x] ｜ 代码：[x]
  - 要点：「图片就是数字矩阵」的直觉；创建、索引、形状（shape）、维度
  - 备注：概念轨已自驱掌握；代码轨 06-23 下午亲手补齐（`np.array([[...]])` 创建、`.shape`、`img[r,c]` 索引、0-based 应用对）。
- [x] **LO1.3 NumPy 广播（broadcasting）**
  - 概念：[x] ｜ 代码：[x]
  - 要点：不同形状数组间的运算规则；广播的发生条件
  - 备注：自行说出「少的一边自动复制去够多的一边」+ 触发条件「维度相等，或其中一边为 1」；(3,)+(3,1)→(3,3) 的双向铺开也理解了。新增入 review_queue 巩固。
- [x] **LO1.4 元素级运算与矩阵运算**
  - 概念：[x] ｜ 代码：[x]
  - 要点：逐元素运算 vs `np.dot`；轴（axis）的概念
  - 备注：`*`（逐元素「分算」）与 `np.dot`（矩阵乘「一排撞一列、乘完求和」）已分清，自行推出 `19=1×5+2×7`。轴（axis）尚未单独讲。新增入 review_queue。
- [x] **LO1.5 matplotlib 基础可视化**
  - 概念：[x] ｜ 代码：[x]
  - 要点：画函数曲线、显示图像（imshow）；为后续观察训练曲线打基础
  - 备注：06-23 晚亲手覆盖。`np.arange(0,6,0.1)`（自纠右开区间「<6 即 5.9」）、`np.sin(x)` 逐元素生成 y、`plt.plot(x,y)`「60 个离散点连线」、`plt.xlabel/ylabel/title/legend`、`plt.imshow` 把数字矩阵渲染成彩色方块（数值→颜色映射：255 最亮/0 最暗/128 居中，自行预测命中）。课末完成 Ch.1 回看闭环：图片⇄数字双向、同一回事。附带环境配置实操：`python -m pip install matplotlib` 修复（pip 在终端跑 vs Python 内、launcher 路径报错）。

---

## 易错点（供 P3 陷阱参考）

- 像素被理解为模糊的"颜色点"而非具体数字
- 把 `*`（逐元素）和 `np.dot`（矩阵乘）混淆
- 广播规则的方向理解反了

## 关联复习

本章是后续所有章节的工具基础。LO1.2/1.3/1.4 在每一章都会反复用到，巩固优先级最高。
