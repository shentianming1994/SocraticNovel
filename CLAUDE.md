# SocraticNovel — 活跃系统入口

本仓库当前运行的教学系统是 **DL_FromScratch**（《深度学习入门》苏格拉底家教）。

**会话开始时，必须立即读取并执行以下文件的指令：**

```
DL_FromScratch/copilot-instructions.md
```

该文件包含完整的启动顺序。读取后按其三步加载流程执行，进入教学模式。

**不要加载** AP_Physics_EM/ 目录下的任何文件——那是参考实现档案，不是活跃系统。
